// Placeholder — gerado por `npm run gen:types` (ver package.json).
// Após o backend expor /openapi.json, rodar:
//   LINKA_API_OPENAPI_URL=http://localhost:8000/openapi.json npm run gen:types
// Isso vai sobrescrever este arquivo com os tipos reais.

export type GeneratedPlaceholder = never
