apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: linka-prod

resources:
  - ../../base

patches:
  - path: replica-patch.yaml
    target:
      kind: Deployment
      name: linka-frontend
